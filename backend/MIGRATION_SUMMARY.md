# ✅ Миграция на PostgreSQL - Полный список файлов

## 📦 Что было создано

### Основные файлы для работы

#### 1. **db_postgres.py** (🔧 Основной модуль)
- Модуль для подключения и работы с PostgreSQL
- Содержит `PostgresConnection` и `PostgresDatabase` классы
- Все методы совместимы с API приложения

#### 2. **db_adapter.py** (🔄 Адаптер)
- Выбирает между JSON и PostgreSQL на основе .env
- Использует существующий API
- Полностью совместим с main.py

#### 3. **migrate_to_postgres.py** (🚀 Скрипт миграции)
- Переносит ВСЕ данные из database.json в PostgreSQL
- Показывает подробный лог и статистику
- Безопасен - не удаляет исходные данные

#### 4. **db_switch.py** (🎛️ Утилита переключения)
- Быстрое переключение между JSON и PostgreSQL
- Команды: status, to-postgres, to-json, migrate, help

#### 5. **schema.sql** (📐 SQL схема)
- Полная схема БД с 15+ таблицами
- Индексы, Foreign Keys, constraints
- Готова к использованию

### Конфигурационные файлы

#### 6. **.env.example** (⚙️ Пример конфигурации)
- Пример всех необходимых переменных
- Комментарии с объяснениями

#### 7. **docker-compose.yml** (🐳 Docker конфиг)
- PostgreSQL 15 контейнер
- PgAdmin для управления БД
- Health checks и автоматический перезапуск

### Документация

#### 8. **POSTGRES_QUICKSTART.md** ⚡ 
- Быстрый старт (5 минут)
- Для нетерпеливых
- Основные команды и проверки

#### 9. **MIGRATION_GUIDE.md** 📚
- Полный пошаговый гайд (30 минут)
- Для тех, кто хочет понять все детали
- Проблемы и их решения

#### 10. **SERVER_MIGRATION_STEPS.md** 🖥️
- Инструкции для боевого сервера
- Установка PostgreSQL на Linux
- Настройка для продакшена

#### 11. **FILES_OVERVIEW.md** 📋
- Описание всех файлов проекта
- Как их использовать
- Рекомендуемый порядок прочтения

#### 12. **README_BACKEND.md** 📖
- Общий README для backend
- Быстрый старт
- API endpoints
- Конфигурация

### Обновленные файлы

#### 13. **main.py** (обновлён)
- Изменён импорт с `from database import db` на `from db_adapter import db`
- Использует адаптер для выбора JSON/PostgreSQL

#### 14. **requirements.txt** (обновлён)
- Добавлены: psycopg2-binary, asyncpg, sqlalchemy

---

## 🚀 Быстрый старт (за 5 минут)

```bash
cd vs-tools/backend

# 1. Запустить PostgreSQL
docker-compose up -d

# 2. Запустить миграцию
python migrate_to_postgres.py

# 3. Переключиться на PostgreSQL
python db_switch.py to-postgres

# 4. Перезапустить приложение
pkill -f "uvicorn main:app"
python main.py
```

---

## 📖 Рекомендуемый порядок изучения

### Для быстрого старта (5-15 минут)
1. **Прочитать** POSTGRES_QUICKSTART.md
2. **Запустить** docker-compose up -d
3. **Выполнить** python db_switch.py migrate
4. **Проверить** python db_switch.py status
5. **Переключиться** python db_switch.py to-postgres

### Для глубокого понимания (30-60 минут)
1. **Прочитать** POSTGRES_QUICKSTART.md
2. **Прочитать** FILES_OVERVIEW.md
3. **Прочитать** MIGRATION_GUIDE.md
4. **Изучить** db_adapter.py
5. **Изучить** db_postgres.py

### Для развёртывания на сервере (1-2 часа)
1. **Прочитать** SERVER_MIGRATION_STEPS.md
2. **Прочитать** MIGRATION_GUIDE.md
3. **Подготовить** сервер
4. **Запустить** миграцию
5. **Протестировать** все API endpoints

---

## ✨ Основные возможности

### ✅ Что работает сразу
- Миграция всех данных без потерь
- Автоматический выбор БД (JSON/PostgreSQL)
- Полная совместимость с существующим API
- Откат на JSON в любой момент
- Встроенные проверки целостности данных

### ✅ Инструменты
- Быстрое переключение между БД (1 команда)
- Проверка статуса и количества записей
- Скрипт миграции с подробным логом
- Docker конфиг для локального тестирования
- Примеры конфигурации

### ✅ Документация
- 5 подробных гайдов
- Пошаговые инструкции
- Решения частых проблем
- Примеры команд
- Чек-листы

---

## 📊 Что переносится

### ✔️ Таблицы пользователей
- users (все пользователи)
- user statuses (онлайн/оффлайн)

### ✔️ Таблицы чатов
- chats (все чаты)
- chat_participants (участники)
- messages (все сообщения)
- message_reactions (реакции)

### ✔️ Таблицы контента
- data_sources (источники)
- feeds (ленты)
- products (товары/туры)
- templates (шаблоны)
- collections (подборки)

### ✔️ Таблицы системы
- settings (настройки)
- logs (логи)
- analytics (аналитика)
- wordstat_searches (поиски)
- tracked_posts (отслеживание)

---

## 🔒 Безопасность данных

### Защита от потерь
- Автоматическая резервная копия database.json
- Проверка целостности данных перед миграцией
- Блокировка если данные не совпадают

### На продакшене
- Отдельный пользователь БД (не postgres)
- SSL подключение
- Регулярные резервные копии
- Мониторинг подключений
- Ограничения по IP

---

## 🛠️ Команды которые нужны

### Для локальной разработки
```bash
# Запустить PostgreSQL
docker-compose up -d

# Проверить статус
python db_switch.py status

# Запустить миграцию
python db_switch.py migrate

# Переключиться на PostgreSQL
python db_switch.py to-postgres

# Вернуться на JSON
python db_switch.py to-json
```

### Для продакшена
```bash
# SSH на сервер
ssh user@server.com

# Установить PostgreSQL
sudo apt install postgresql postgresql-contrib

# Создать БД
sudo -u postgres psql < /tmp/setup.sql

# Запустить миграцию (с локальной машины)
python migrate_to_postgres.py

# Переключить приложение
python db_switch.py to-postgres

# Перезапустить
sudo systemctl restart shar-backend
```

---

## 📞 Если что-то не работает

### PostgreSQL не подключается
```bash
# Проверить что он запущен
docker ps

# Смотреть логи
docker-compose logs postgres

# Перезапустить
docker-compose restart postgres
```

### Миграция не переносит данные
```bash
# Проверить логи
python migrate_to_postgres.py 2>&1 | tee migration.log

# Проверить что данные в database.json
python3 << 'EOF'
import json
with open('database.json') as f:
    data = json.load(f)
    print(f"Users: {len(data.get('users', []))}")
    print(f"Chats: {len(data.get('chats', []))}")
    print(f"Messages: {len(data.get('messages', []))}")
EOF
```

### API не работает после переключения
```bash
# Проверить что приложение запущено
ps aux | grep "uvicorn main:app"

# Смотреть логи
tail -50 backend.log | grep ERROR

# Проверить конфигурацию БД
python db_switch.py status

# Если нужен откат
python db_switch.py to-json
systemctl restart shar-backend
```

---

## 🎯 Итоговый чек-лист

Перед началом:
- [ ] Прочитан POSTGRES_QUICKSTART.md
- [ ] Создана резервная копия database.json
- [ ] Docker установлен (если локально)
- [ ] Все зависимости установлены

Во время миграции:
- [ ] PostgreSQL запущен
- [ ] Миграция завершена без ошибок
- [ ] Статус показывает правильные числа
- [ ] API endpoints работают

После миграции:
- [ ] Все данные видны в приложении
- [ ] Тестовое сообщение отправлено
- [ ] Логи проверены на ошибки
- [ ] Резервные копии настроены

---

## 🎉 Готово!

Теперь у вас есть:

✅ Полностью миграционный pipeline  
✅ Поддержка PostgreSQL  
✅ Откат на JSON в любой момент  
✅ Подробная документация  
✅ Инструменты управления  
✅ Примеры конфигурации  
✅ Решения проблем  

**Начните с POSTGRES_QUICKSTART.md и выполните 5 команд - всё будет готово!** 🚀
