# 📦 Миграция на PostgreSQL - Созданные файлы

## 📄 Документация

### 1. **POSTGRES_QUICKSTART.md** ⚡ НАЧНИ ОТСЮДА
Быстрый старт за 5 минут. Самый короткий путь к миграции.

**Содержит:**
- Быстрый чек-лист команд
- Базовая конфигурация
- Проверка статуса
- Откат в случае проблем

**Используй если:** Нужно быстро понять что делать

---

### 2. **MIGRATION_GUIDE.md** 📚 ПОЛНЫЙ ГАЙД
Подробное пошаговое описание всего процесса миграции.

**Содержит:**
- Подготовка к миграции
- Установка PostgreSQL (локально и на сервере)
- Инициализация схемы БД
- Запуск миграции
- Проверка после миграции
- Откат на JSON
- Мониторинг
- Продакшн настройки
- Чек-лист
- Частые проблемы и их решения

**Используй если:** Хочешь понять всё в деталях

---

### 3. **SERVER_MIGRATION_STEPS.md** 🖥️ ДЛЯ СЕРВЕРА
Пошаговые инструкции специально для боевого сервера.

**Содержит:**
- Подготовка сервера
- Установка PostgreSQL на сервер
- Создание БД и пользователя
- Инициализация схемы
- Установка зависимостей
- Конфигурация для миграции
- Запуск миграции с локальной машины
- Тестирование
- Переключение сервера на PostgreSQL
- Мониторинг
- Откат

**Используй если:** Мигрируешь данные на реальный сервер

---

## 🔧 Скрипты и утилиты

### 4. **db_postgres.py** 🗄️ МОДУЛЬ POSTGRESQL
Основной модуль для работы с PostgreSQL БД.

**Содержит:**
- `PostgresConnection` - класс для подключения к БД
- `PostgresDatabase` - класс со всеми операциями
- Методы для работы с:
  - Users, Chats, Messages
  - Products, Feeds, DataSources
  - Settings, Templates, Collections
  - Logs, Analytics, и т.д.

**Использование:**
```python
from db_postgres import PostgresConnection, PostgresDatabase

conn = PostgresConnection(host='localhost', user='postgres')
conn.connect()
db = PostgresDatabase(conn)

users = db.get_users()
```

---

### 5. **db_adapter.py** 🔄 АДАПТЕР JSON/POSTGRES
Умный адаптер, который выбирает между JSON и PostgreSQL.

**Содержит:**
- `DatabaseAdapter` - класс, переключающийся на PostgreSQL
- Fallback на JSON если PostgreSQL недоступен
- Все методы совместимы с существующим API

**Использование в main.py:**
```python
from db_adapter import db  # Вместо from database import db

# Работает одинаково - выбирает БД автоматически
users = db.get_users()
```

---

### 6. **migrate_to_postgres.py** 🚀 СКРИПТ МИГРАЦИИ
Главный скрипт для переноса данных из JSON в PostgreSQL.

**Возможности:**
- Загружает все данные из database.json
- Подключается к PostgreSQL
- Создаёт все таблицы (если нужно)
- Переносит все данные по категориям:
  - Users, Chats, Messages
  - Products, DataSources, Feeds
  - Templates, Collections, Settings
- Показывает подробный лог процесса
- Выводит статистику

**Запуск:**
```bash
python migrate_to_postgres.py
```

---

### 7. **db_switch.py** 🎛️ ПЕРЕКЛЮЧАТЕЛЬ БД
Удобная утилита для управления переключением между БД.

**Команды:**
```bash
# Показать текущий статус
python db_switch.py status

# Переключиться на PostgreSQL
python db_switch.py to-postgres

# Переключиться на JSON
python db_switch.py to-json

# Запустить миграцию
python db_switch.py migrate

# Справка
python db_switch.py help
```

**Особенности:**
- Проверяет подключение к БД перед переключением
- Показывает количество записей в каждой таблице
- Валидирует конфигурацию

---

## 📋 Конфигурационные файлы

### 8. **schema.sql** 📐 SQL СХЕМА
Полная SQL схема всех таблиц PostgreSQL.

**Содержит:**
- 15+ таблиц для всех данных приложения
- Индексы для быстрых запросов
- Foreign Keys для связей
- Default значения

**Использование:**
```bash
psql -h localhost -U postgres -d shar_messenger < schema.sql
```

---

### 9. **.env.example** ⚙️ ПРИМЕР КОНФИГУРАЦИИ
Пример переменных окружения для PostgreSQL.

**Содержит:**
- DB_HOST, DB_PORT, DB_NAME
- DB_USER, DB_PASSWORD
- USE_POSTGRES флаг
- KEEP_JSON_BACKUP флаг

**Использование:**
```bash
# Создать .env из примера
cp .env.example .env

# Отредактировать
nano .env
```

---

### 10. **docker-compose.yml** 🐳 DOCKER КОНФИГ
Конфиг для быстрого запуска PostgreSQL в Docker.

**Содержит:**
- PostgreSQL 15 контейнер
- PgAdmin 4 для управления БД через UI
- Томи для персистентных данных
- Health checks

**Использование:**
```bash
# Запустить PostgreSQL
docker-compose up -d

# Проверить
docker ps

# Остановить
docker-compose down

# PgAdmin доступен на http://localhost:5050
# Логин: admin@local.dev / admin123
```

---

## 🔄 Обновленные файлы

### 11. **main.py** (обновлён)
- Изменен импорт с `from database import db` на `from db_adapter import db`
- Теперь автоматически выбирает JSON или PostgreSQL на основе .env
- Все остальное работает без изменений

---

### 12. **requirements.txt** (обновлён)
Добавлены зависимости:
- `psycopg2-binary` - драйвер PostgreSQL
- `asyncpg` - асинхронный драйвер PostgreSQL
- `sqlalchemy` - ORM (опционально, на будущее)

---

## 📊 Работа с файлами

### Для локальной разработки:
1. **Запустить PostgreSQL:**
   ```bash
   docker-compose up -d
   ```

2. **Проверить статус:**
   ```bash
   python db_switch.py status
   ```

3. **Запустить миграцию:**
   ```bash
   python db_switch.py migrate
   ```

4. **Переключиться на PostgreSQL:**
   ```bash
   python db_switch.py to-postgres
   ```

5. **Перезапустить приложение:**
   ```bash
   pkill -f "uvicorn main:app"
   python main.py
   ```

---

### Для боевого сервера:
1. **Следовать инструкциям в SERVER_MIGRATION_STEPS.md**
2. **Основные шаги:**
   - Установить PostgreSQL на сервер
   - Создать БД и пользователя
   - Выполнить schema.sql
   - Запустить миграцию с локальной машины
   - Переключить сервер на PostgreSQL
   - Перезапустить приложение

---

## 🎯 Рекомендуемый порядок прочтения

1. **Если спешишь:** POSTGRES_QUICKSTART.md (5 мин)
2. **Если хочешь разобраться:** MIGRATION_GUIDE.md (20 мин)
3. **Если мигрируешь на продакшен:** SERVER_MIGRATION_STEPS.md + MIGRATION_GUIDE.md

---

## ✅ Что я получу после миграции?

✔️ Все данные безопасно в PostgreSQL  
✔️ Приложение работает быстрее  
✔️ Можно делать сложные SQL запросы  
✔️ Автоматические резервные копии  
✔️ Масштабируемость  
✔️ Лучший мониторинг  
✔️ Легче разбираться с данными  

---

## 🚀 Начни уже сейчас!

```bash
cd vs-tools/backend

# Вариант 1: Быстро (5 мин)
docker-compose up -d
python db_switch.py migrate
python db_switch.py to-postgres

# Вариант 2: Понимая (20 мин)
# Прочитай POSTGRES_QUICKSTART.md
# Затем повтори команды

# Вариант 3: На продакшене
# Прочитай SERVER_MIGRATION_STEPS.md
# Следуй шаг за шагом
```

**Удачи с миграцией! 🎉**
