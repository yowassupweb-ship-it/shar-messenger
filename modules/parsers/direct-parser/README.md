# Парсер Яндекс Рекламы 🔍

Веб-приложение для парсинга контекстной и поисковой рекламы конкурентов в Яндексе.

## ✨ Возможности

- 🎯 **Парсинг рекламы Яндекс.Директ** - контекстная и поисковая реклама
- 📄 **Множественные запросы** - обработка нескольких поисковых запросов одновременно
- 📊 **Настройка глубины парсинга** - от 1 до 10 страниц результатов на запрос
- 🌐 **Два режима работы** - видимый и скрытый браузер
- 📥 **Экспорт в 3 форматах**:
  - **JSON** - структурированные данные
  - **CSV** - таблица для Excel
  - **XML** - универсальный формат обмена данными
- 📈 **Реал-тайм мониторинг** - прогресс-бар и детальные логи
- 🎨 **Современный UI** - темный стеклянный дизайн

## Установка

### 1. Установите зависимости:
```bash
pip install -r requirements.txt
```

Это установит:
- **Flask** - веб-фреймворк
- **Selenium** - автоматизация браузера
- **webdriver-manager** - автоматическое управление ChromeDriver

## Запуск

### 1. Запустите веб-сервер:
```bash
python app.py
```

### 2. Откройте браузер:
Перейдите по адресу: **http://127.0.0.1:5000**

### 3. Используйте веб-интерфейс:
1. Введите поисковые запросы (каждый с новой строки)
2. Настройте количество страниц для парсинга (1-10)
3. Выберите режим работы браузера
4. Нажмите "Запустить парсинг"
5. Наблюдайте за прогрессом в реальном времени
6. Скачайте результаты в **JSON**, **CSV** или **XML**

## Веб-интерфейс

### Основные возможности:
- 📝 Удобный ввод множественных запросов
- 📊 Отслеживание прогресса в реальном времени
- 🎯 Настройка параметров парсинга
- 📈 Визуализация результатов в таблице
- 💾 Экспорт в JSON, CSV и XML форматы
- 🚀 Фоновое выполнение парсинга
- 🎨 Темный стеклянный дизайн

### Настройки:
- **Количество страниц**: от 1 до 10 страниц результатов
- **Скрытый режим**: браузер работает в фоне (быстрее)
- **Обычный режим**: видимый браузер (для отладки)

## Результаты

Скрипт сохраняет результаты в трех форматах:

- **yandex_ads_TIMESTAMP.json** - формат JSON для программной обработки
- **yandex_ads_TIMESTAMP.csv** - формат CSV для открытия в Excel
- **yandex_ads_TIMESTAMP.xml** - формат XML для универсального обмена данными

### Структура данных:

```json
{
  "platform": "Яндекс",
  "type": "Контекстная реклама",
  "query": "купить ноутбук",
  "title": "Заголовок объявления",
  "description": "Текст объявления",
  "url": "https://...",
  "display_url": "example.com",
  "timestamp": "2025-11-12 10:30:45"
}
```

### Пример XML:

```xml
<?xml version="1.0" encoding="utf-8"?>
<ads timestamp="20251112_143000" total="42">
  <ad>
    <platform>Яндекс</platform>
    <type>Контекстная реклама</type>
    <query>туры по россии</query>
    <title>Туры по России от прямых организаторов</title>
    <description>Авторские туры. Все включено...</description>
    <url>https://example.com</url>
    <display_url>example.com</display_url>
    <timestamp>2025-11-12 14:36:49</timestamp>
  </ad>
</ads>
```

## Примечания

- Убедитесь, что у вас стабильное интернет-соединение
- Слишком частые запросы могут привести к блокировке IP
- Используйте задержки между запросами (time.sleep)
- Для больших объемов данных рекомендуется использовать прокси
- Селекторы могут устареть при изменении структуры сайтов

## API Endpoints

Веб-приложение предоставляет REST API:

- `GET /` - Главная страница
- `POST /start_parsing` - Запуск парсинга
- `GET /status` - Текущий статус парсинга
- `GET /results` - Получение результатов
- `GET /download_json` - Скачать JSON
- `GET /download_csv` - Скачать CSV
- `GET /download_xml` - Скачать XML

## Расширенное использование

### Использование CLI версии:

Также доступен скрипт `ad_parser.py` для запуска из командной строки:

```bash
python ad_parser.py
```

### Настройка прокси в app.py:

Отредактируйте класс `YandexAdParser`:

```python
self.chrome_options.add_argument('--proxy-server=http://your-proxy:port')
```

## Устранение проблем

**Ошибка: ChromeDriver не найден**
- Установите webdriver-manager или добавьте ChromeDriver в PATH

**Элементы не находятся**
- Увеличьте time.sleep() после загрузки страниц
- Проверьте, не изменились ли селекторы на сайтах

**Captcha**
- Используйте прокси
- Увеличьте задержки между запросами
- Рассмотрите использование сервисов решения captcha
