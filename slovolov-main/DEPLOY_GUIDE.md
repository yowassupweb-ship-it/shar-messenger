# 🚀 Деплой Словолов на Vercel

## Шаг 1: Подготовка к деплою

Убедитесь, что все файлы готовы:
- ✅ `middleware.ts` - аутентификация
- ✅ `vercel.json` - конфигурация
- ✅ `.env.example` - пример переменных
- ✅ Все API routes используют environment variables

## Шаг 2: Деплой на Vercel

### Вариант 1: Через CLI
```bash
# Установить Vercel CLI
npm install -g vercel

# Войти в аккаунт
vercel login

# Задеплоить проект
vercel
```

### Вариант 2: Через GitHub + Vercel Dashboard
1. Push код в GitHub репозиторий
2. Зайти на [vercel.com](https://vercel.com)
3. Подключить GitHub репозиторий
4. Vercel автоматически задеплоит проект

## Шаг 3: Настройка Environment Variables

В Vercel Dashboard → Project Settings → Environment Variables добавить:

```env
# Основные настройки API
YANDEX_WORDSTAT_TOKEN=ваш_реальный_oauth_токен_яндекс
YANDEX_WORDSTAT_API_URL=https://api.wordstat.yandex.net

# Аутентификация приложения
ADMIN_USERNAME=admin
ADMIN_PASSWORD=vstravel995

# Безопасность Next.js
NEXTAUTH_SECRET=super-secret-production-key-2024-slovolov
NEXTAUTH_URL=https://your-domain.vercel.app

# Настройки приложения
DEFAULT_RESULTS_LIMIT=500
MAX_RESULTS_LIMIT=10000
```

⚠️ **ВАЖНО**: Обязательно замените:
- `ваш_реальный_oauth_токен` на реальный токен Yandex
- `your-app-name.vercel.app` на реальный URL вашего приложения

## Шаг 4: Проверка деплоя

После деплоя проверьте:
1. ✅ Логин работает (admin / vstravel995)
2. ✅ API статус показывает подключение
3. ✅ Поиск по ключевым словам работает
4. ✅ Экспорт в CSV работает

## Готовые файлы для деплоя

Все необходимые файлы уже настроены:
- `src/middleware.ts` - Basic Auth
- `vercel.json` - безопасность и конфигурация
- API routes - используют environment variables
- `.env.example` - документация переменных

## Команды для локального тестирования

```bash
# Запуск dev сервера
npm run dev

# Сборка проекта
npm run build

# Запуск production сборки
npm start
```

## 🎯 Результат

После успешного деплоя у вас будет:
- 🔐 Защищенное приложение (Basic Auth)
- 📊 Анализ ключевых слов Yandex Wordstat
- 📈 Пакетная обработка запросов
- 📄 Экспорт результатов в CSV
- 🌍 Доступ из любой точки мира

**URL после деплоя**: `https://your-app-name.vercel.app`