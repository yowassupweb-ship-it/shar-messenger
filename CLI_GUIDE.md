# CLI Documentation - Вокруг света

Удобный инструмент командной строки для управления проектом.

## 🚀 Быстрый старт

```powershell
# Запустить весь проект в dev режиме
.\cli.ps1 dev

# Проверить статус сервисов
.\cli.ps1 status

# Остановить все сервисы
.\cli.ps1 stop both
```

## 📋 Полный список команд

### Управление сервисами

```powershell
# Запуск
.\cli.ps1 start [backend|frontend|both]
.\cli.ps1 dev                          # Режим разработки (оба сервиса)

# Остановка
.\cli.ps1 stop [backend|frontend|both]

# Перезапуск
.\cli.ps1 restart [backend|frontend|both]

# Статус
.\cli.ps1 status                       # Показать состояние всех сервисов
```

### Разработка

```powershell
# Сборка
.\cli.ps1 build                        # Собрать для продакшена

# Тесты
.\cli.ps1 test [backend|frontend|both] # Запустить тесты

# Линтинг
.\cli.ps1 lint [backend|frontend|both] # Проверить код
```

### База данных

```powershell
# Резервные копии
.\cli.ps1 db:backup                    # Создать бэкап в ./backups/
.\cli.ps1 db:restore <файл>            # Восстановить из файла

# Управление данными
.\cli.ps1 db:reset                     # Сбросить БД (будет добавлено)
.\cli.ps1 db:seed                      # Заполнить тестовыми данными (будет добавлено)
```

### Зависимости

```powershell
# Установка
.\cli.ps1 install                      # Установить все зависимости (npm + pip)

# Обслуживание
.\cli.ps1 clean                        # Очистить кеш (__pycache__, etc.)
```

### Информация

```powershell
.\cli.ps1 info                         # Информация о проекте
.\cli.ps1 health                       # Проверить установку Python/Node/npm
.\cli.ps1 help                         # Показать справку
```

## 📖 Примеры использования

### Типичный рабочий процесс

```powershell
# 1. Установить зависимости (первый запуск)
.\cli.ps1 install

# 2. Проверить здоровье системы
.\cli.ps1 health

# 3. Запустить в dev режиме
.\cli.ps1 dev

# 4. Проверить статус
.\cli.ps1 status

# 5. Сделать бэкап БД перед изменениями
.\cli.ps1 db:backup

# 6. Работа... работа... работа...

# 7. Остановить сервисы
.\cli.ps1 stop both
```

### Продакшен сборка

```powershell
# Подготовка к деплою
.\cli.ps1 db:backup                    # Бэкап данных
.\cli.ps1 build                        # Сборка frontend
.\cli.ps1 test both                    # Запуск тестов

# Запуск в продакшене
.\cli.ps1 start both
```

### Отладка

```powershell
# Запустить только backend для отладки
.\cli.ps1 start backend
.\cli.ps1 status

# Перезапустить frontend
.\cli.ps1 restart frontend

# Посмотреть логи через API
# GET http://localhost:8000/api/logs
```

## 🔧 Альтернативный скрипт запуска

Для более детального контроля используйте `start-server.ps1`:

```powershell
# Запуск
.\start-server.ps1 -Both
.\start-server.ps1 -Backend
.\start-server.ps1 -Frontend

# Остановка
.\start-server.ps1 -Stop -Both

# Перезапуск
.\start-server.ps1 -Restart -Backend

# Статус
.\start-server.ps1 -Status
```

## 🛠️ Технические детали

### Проверка портов

CLI автоматически проверяет доступность портов:
- **Backend**: 8000
- **Frontend**: 3000

### Логирование

- Цветной вывод в консоль (зеленый = успех, красный = ошибка, желтый = предупреждение)
- Backend логи доступны через API: `GET /api/logs`

### Бэкапы

- Создаются в папке `./backups/`
- Формат имени: `database_YYYY-MM-DD_HH-mm-ss.json`
- Восстановление с подтверждением (вводите `yes`)

## 💡 Советы

1. **Всегда делайте бэкап** перед изменением структуры БД
2. **Используйте `status`** для проверки запущенных сервисов
3. **`health` полезен** для диагностики проблем с окружением
4. **`clean` освободит место**, удалив кеш Python
5. **`info` покажет** все URL и версии технологий

## ❓ FAQ

**Q: Как остановить все сервисы сразу?**
```powershell
.\cli.ps1 stop both
```

**Q: Порт уже занят, что делать?**
```powershell
# Остановите процессы вручную или перезагрузите систему
Get-NetTCPConnection -LocalPort 8000 | Select-Object OwningProcess
Stop-Process -Id <процесс_id>
```

**Q: Как проверить версии Python/Node?**
```powershell
.\cli.ps1 health
```

**Q: Где хранятся данные?**
```
backend/database.json - основная БД
backups/ - резервные копии
```

**Q: Как добавить новую команду в CLI?**
Отредактируйте `cli.ps1`, добавьте функцию и добавьте case в switch statement внизу файла.

## 📞 Поддержка

Для получения справки:
```powershell
.\cli.ps1 help
```

Или откройте issue в репозитории проекта.
